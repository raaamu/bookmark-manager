package com.example.bookmarkmanager.security;

import org.springframework.context.annotation.Configuration;

// This class is intentionally left empty to avoid conflicts with SecurityConfig.java
// We're not using @EnableWebSecurity here to prevent duplicate security configurations
@Configuration
public class WebSecurityConfig {
    // No beans defined here to avoid conflicts
}
